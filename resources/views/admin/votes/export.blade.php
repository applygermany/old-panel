<div class="table-responsive">    <table class="table">        <thead class="table-light">        <tr class="text-center">            <th>نام و نام خانوادگی</th>            <th>کارشناس</th>            <th>تاریخ ثبت</th>            @foreach($questions as $question)                <th>{{$question}}</th>            @endforeach            <th>نظر شما</th>            <th>انتقاد</th>            <th>نوع همکاری</th>        </tr>        </thead>        <tbody>        @foreach($votes as $vote)            <tr>                <td class="text-gray-600">{{ $vote->userName }}</td>                <td class="text-gray-600">{{ $vote->expertName }}</td>                <td class="text-gray-600">{{ $vote->created_at }}</td>                @foreach($questions as $id => $question)                    <td class="text-gray-600">{{isset($vote->answers[$id]) ? $vote->answers[$id] : "ثبت نشده"}}</td>                @endforeach                <td class="text-gray-600">{{ $vote->comment }}</td>                <td class="text-gray-600">{{ $vote->recommend }}</td>                <td class="text-gray-600">{{ $vote->types }}</td>            </tr>        @endforeach        </tbody>    </table></div>